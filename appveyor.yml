branches:
  only:
    - master
install:
    - ps: Install-Product node 6.9.1 x64
    - npm install -g npm --silent
    - npm install --force

build_script:
    - npm run build --silent

test_script:
    - npm run test --silent
