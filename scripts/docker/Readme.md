# Root for docker files used in BatchExplorer build

Those files have the required software installed to build batch explorer from source

    * python
    * node
    * git

There is both a windows and linux docker image
